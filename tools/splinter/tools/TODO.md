* [x] if the script is installed within the `splinter/tools` directory then it will accept a parameter to know where to store the output
* [ ] add check that the keychain is not stored in the Library
